# 相控阵雷达全阶段课程讲义（讲解版）

> 配套《相控阵雷达学习方案.md》。版本：2026-08-18
> 模式：纯讲解，零执行。每课结构 = 目标 / 大白话 / 核心原理与公式 / 关键点坑 / 仿真看什么。
> 主线：雷达 = 一条信号的旅程（发射→传播→散射→接收→处理→决策）。相控阵让旅程的"角度维度"可编程。
> 硬性验收：每课末尾的"仿真看什么"和练习，要在进入下一课前用 Python 跑通（动手层仍按总纲执行）。

---

# 阶段1 雷达全景与物理地基（L1 会看）

## 课1 雷达是什么 + 电磁波物理
**目标**：讲清"雷达=电磁回声测距"；掌握 f/λ/c 三角关系。
**大白话**：雷达是"用电磁波喊的蝙蝠"——发射电磁波，碰目标弹回，测往返时间算距离。
**核心原理**：
- 真空中电磁波速度 c = 3×10⁸ m/s。核心关系 **c = λ·f**。
- 雷达频段按波长命名：L(1–2GHz,~30cm)、S(2–4,~10cm)、C(4–8)、X(8–12,~3cm)、Ku(12–18)、Ka(26–40)。
- 选微波的原因：波长太长→波束宽、目标散射弱；太短→雨衰大、器件难；微波是折中。
- 极化（电场振动方向）和传播衰减（扩散+吸收）先埋点，课3/课2 分别展开。
**关键点/坑**：
- 坑1：频率越高波长越短（直觉常反），X=3cm 和 L=30cm 要脱口而出。
- 坑2：雷达波是电磁波，不需要介质——和声呐（水声）是两回事，别混。
**仿真看什么**：画各频段空间波长对比图（L/S/X 三种尺度叠一起）；X 波段固定点电场振荡图，看周期 T=100ps。

## 课2 雷达方程（系统能量账）
**目标**：能逐项讲雷达方程，理解"距离四次方"的物理后果。
**大白话**：把雷达想象成手电筒照镜子——光出去又回来，越远越弱，且衰减是"去程衰减×回程衰减"的叠加。
**核心原理与公式**：

$$P_r = \frac{P_t\, G_t\, G_r\, \lambda^2\, \sigma}{(4\pi)^3\, R^4}$$

- P_t 发射功率；G_t 发射增益；G_r 接收增益；λ² 由天线口径决定；σ 目标 RCS（散射截面积）；R⁴ 两程损耗。
- 三个推论：
  1. **R⁴**：探测距离 R_max ∝ P^(1/4)，功率翻16倍距离才翻1倍——功率是最钝杠杆。
  2. **口径是王道**：λ²G 组合实质是物理口径 A_e，所以"雷达拼阵面"有数学依据。
  3. **σ 是敌人送的**：隐身压 σ 几个数量级 = 直接削 R。
- 含噪声的系统形式：SNR = P_r / (k·T₀·B·F·L)，F 噪声系数(阶段6课2)、L 损耗。
**关键点/坑**：
- 坑1：忘掉"两程"就会把 (4π)² 当 (4π)³，回波弱 4π 倍。
- 坑2：σ 不是物理截面积，是"等效各向同性散射面积"，随频率/姿态剧变。
**仿真看什么**：SNR vs 距离曲线（对数坐标直线斜率 -4），看 R⁴ 悬崖；改 σ 看曲线平移。

## 课3 雷达体制总览
**目标**：建立"按体制判断能力"的坐标系。
**大白话**：雷达像不同的"问话方式"——有的问"多远"（脉冲），有的问"多快"（连续波），有的两者都问（脉冲多普勒）。
**核心原理**：
- 脉冲雷达：发一脉冲，等回波，测时延→距离。
- 连续波 CW：发连续波，测频移→速度，不能测距；FMCW 调制频率→同时测距测速（车载毫米波主力）。
- MTI：只保留动目标（对消固定杂波）；脉冲多普勒 PD：多脉冲 FFT 得距离-速度二维。
- 按天线：机械扫描 / 相控阵（电扫描）/ MIMO。
**关键点/坑**：
- 坑：看到"雷达"先问三件事——什么波形？测距测速能不能同时？天线怎么扫？
**仿真看什么**：不用仿，本课是坐标系；画一张"体制×能力"对照图贴笔记本。

## 课4 系统组成与信号流
**目标**：能默画完整框图，每框一句话说清干什么。
**核心原理（信号旅程的固定站名）**：
发射机 → 天线 → 传播 → 目标散射 → 接收机(LNA→混频→ADC) → 信号处理(脉压→多普勒→CFAR) → 数据处理(关联→跟踪) → 决策/显示。
**关键点/坑**：
- 坑：信号处理和数据处理是两回事——前者在"波形域"提点迹，后者在"目标域"建航迹。混了后面全乱。
**仿真看什么**：本课画框图；每个框标"哪个阶段哪课管它"（这是你的学习地图）。

## 课5 距离/速度/角度三个测量
**目标**：掌握测距测速测角的基本原理与公式。
**核心原理**：
- 距离：往返时延 τ → **R = c·τ/2**。
- 速度：多普勒频移 **f_d = 2v/λ**（接近为负频移/远离为正，方向视定义）。
- 角度：单天线靠波束方向，阵列靠阵元间相位差（阶段3）。
**关键点/坑**：
- 坑：测距公式÷2 忘掉 = 距离差一倍。去+回两趟。
**仿真看什么**：课1已做测距仿真；补一个多普勒仿真（正弦加频移，FFT 看峰值位置→反算速度）。

## 课6 相控阵 vs 机械扫描
**目标**：理解相控阵"买的是时间自由度"。
**核心原理**：
- 机械：转天线，帧率低、惯性大、单波束。
- 相控阵：电子移相瞬间跳波束 → 搜索+跟踪交替(TWS)、多目标同时跟踪、频率捷变、波形灵活。
- 代价：TR 组件海量（贵）、扫描时波束展宽+增益掉 cosθ、要压栅瓣。
**关键点/坑**：
- 坑：相控阵不等于"一定更好"——静止小范围监视机械扫描更省钱。
**仿真看什么**：模拟 TWS 时间轴（搜索空域扫一遍 vs 抽时间盯目标），看时间怎么被分走。

**阶段1验收**：默画框图、讲清雷达方程每项、能说三个测量的公式、说清相控阵的代价。

---

# 阶段2 雷达信号与噪声数学（L2 会算）

## 课1 采样与 FFT
**目标**：搞懂"离散化"的世界观——采样、混叠、频谱。
**大白话**：采样就是"拍照"，拍得够快才不丢信息；拍慢了就"混叠"（高频冒充低频）。
**核心原理**：
- 奈奎斯特：fs ≥ 2×带宽 才无混叠。
- DFT/FFT：时域离散→频域离散。频谱分辨率 = fs/N（数据越长越细）。
- 窗函数：截断产生泄漏，加窗控旁瓣但主瓣变宽——**分辨率 vs 旁瓣的第一次折中**（阶段3锥削同逻辑）。
**关键点/坑**：
- 坑1：混叠不可逆，滤波必须在采样前做（抗混叠滤波器）——这是 ADC 前的硬件必修课（阶段6）。
- 坑2：频谱分辨率 vs 采样点数的关系，做 RD 图（课6）时直接决定目标能不能分开。
**仿真看什么**：fs 低于信号频率两倍 → 正弦变低频的"假象"；加窗前后频谱旁瓣对比。

## 课2 复信号与 IQ 解调
**目标**：明白"雷达为什么要用复数干活"。
**大白话**：复信号 = 幅度+相位一起记。相位是后面测角和波束成形的"原料"，记不下相位就什么都干不了。
**核心原理**：
- 实信号频谱正负对称、信息冗余；IQ 变成复基带后只保留单边谱 → 带宽减半、采样率减半。
- **负多普勒可辨**：只有实信号分不出靠近/远离，IQ 能。
- IQ = I(t)+jQ(t)，幅度 √(I²+Q²)，相位 atan2(Q,I)。
**关键点/坑**：
- 坑：IQ 两路幅相不平衡会引入镜像杂散——硬件校准时必查（阶段4课6）。
**仿真看什么**：实正弦 vs 复指数频谱对比；构造 IQ 信号解出正负频率。

## 课3 噪声与 SNR
**目标**：把"SNR"变成肌肉记忆——一切检测的货币。
**核心原理**：
- 热噪声功率 N = k·T₀·B·F（k=1.38×10⁻²³，T₀=290K，B 带宽，F 噪声系数）。
- 雷达检测全由 SNR 驱动：SNR 高→检测概率高、测距测角精度高。
- 复高斯噪声（圆对称）是标准模型。
**关键点/坑**：
- 坑：噪声功率和带宽成正比——"带宽越宽看得越清楚"是错的，带宽带来噪声，真实增益靠匹配滤波(课4)。
**仿真看什么**：生成复高斯噪声，验证统计（实部虚部方差、幅度瑞利分布）。

## 课4 匹配滤波与脉冲压缩（阶段2分水岭）
**目标**：懂匹配滤波"对齐叠加"直觉；掌握脉压把"脉宽与分辨率解耦"。
**大白话**：像把一堆散页按页码整理好再读——匹配滤波把分散在时间里的能量对齐，信噪比最大。
**核心原理**：
- 匹配滤波冲激响应 = 发射波形的时间反转共轭。输出 SNR 最大。
- 矛盾：距离分辨率 ΔR = c·τ_p/2，脉冲越短越好，但能量越少。
- **脉压**：发长脉冲+内部大带宽（chirp），收时压成短脉冲：
  - **ΔR = c/(2B)**（只看带宽！）
  - 压缩比 = B·τ_p = SNR 增益（chirp 100µs、1MHz → 等效 1µs、增益 100 倍=20dB）。
- LFM chirp：频率线性扫过 B。
**关键点/坑**：
- 坑1：旁瓣！脉压输出有 sinc 旁瓣(−13dB)，要加窗(Taylor/汉明)压到 −30dB 以下，代价主瓣稍宽。
- 坑2：chirp 对多普勒敏感（距离-多普勒耦合）——课5 模糊函数讲。
**仿真看什么**：chirp 过匹配滤波 → 峰值变窄变高；加窗前后旁瓣对比。

## 课5 模糊函数（波形设计的心脏）
**目标**：用模糊函数"看"一个波形的性格。
**核心原理**：
- 模糊函数 χ(τ,f_d) = 匹配滤波输出对时延+多普勒的响应。**波形的身份证**。
- 三种波形性格：
  - 矩形脉冲：距离分辨差（无调制），多普勒分辨好（长脉宽）
  - LFM：距离好、多普勒容限宽、**距离-多普勒耦合（斜脊）**
  - 相位编码(Barker)：距离旁瓣可控、多普勒敏感（不适合高速）
- 零多普勒截面 = 距离旁瓣，零时延截面 = 多普勒旁瓣。
**关键点/坑**：
- 坑：没有万能波形；选波形 = 对任务选模糊函数形状。阶段7 认知雷达的"自适应波形"就靠这个。
**仿真看什么**：画 LFM / 矩形脉冲 / Barker 三张模糊函数等高线，看脊的方向。

## 课6 多普勒处理（MTI 与脉冲多普勒）
**目标**：掌握距离-多普勒矩阵；理解 PRF 的模糊性悖论。
**核心原理**：
- MTI：脉冲对消（相减）滤固定杂波——但慢目标也接近零频会被误滤。
- PD：对同距离的多个脉冲做 FFT → 距离-多普勒(RD)图，目标=一个峰。
- **模糊性定理**：
  - 距离不模糊：R_unamb = c/(2·PRF)
  - 速度不模糊：v_max = λ·PRF/4
  - **高 PRF 测速好距离模糊，低 PRF 相反**——脉冲多普勒最核心的权衡，阶段6 系统设计要算。
**关键点/坑**：
- 坑：RD 图里"折叠"的目标（距离或速度模糊）是真实目标，解模糊靠多重 PRF 或相位算法——工程难点。
**仿真看什么**：多脉冲构造 RD 图；改 PRF 看目标在距离轴/速度轴折叠。

**阶段2验收**：从零写脉压并和 RadarSimPy 对拍；画 LFM 模糊函数；生成并解释一帧 RD 图。

---

# 阶段3 天线与阵列理论（L3 会阵列）

## 课1 天线基础
**目标**：建立"口径→增益"直觉。
**核心原理**：
- 增益 G = 4π·A_e/λ²（A_e 有效口径）。口径越大、频率越高 → 增益越高。
- 方向图：主瓣/副瓣/后瓣；方向性与增益同源。
**关键点/坑**：
- 坑：高增益 = 窄波束（能量守恒）——"又要增益高又要覆盖广"不存在，这是天线第一守恒律。
**仿真看什么**：均匀线源/面源方向图（sinc 形状），看口径越大主瓣越窄增益越高。

## 课2 线性阵列与阵列因子（相位差→波束的正史）
**目标**：能手推"指向 θ₀ 要移多少相"。
**大白话**：一排人接力传口号，喊成"斜的"波前——相位差就是这个"错拍"。
**核心原理**：
- 第 n 个单元相对基准相位差：**Δφ_n = -2π·n·d·sinθ/λ**
- 指向 θ₀：按 Δφ_n = -2π·n·d·sinθ₀/λ 移相。
- 阵列因子 AF = sin(Nψ/2)/sin(ψ/2)，ψ = 2πd(sinθ−sinθ₀)/λ。主瓣在 ψ=0。
- **栅瓣**：d/λ·(sinθ−sinθ₀) 跨整数 → 第二个峰。**d ≤ λ/2 保安全**（半波长间距的来历）。
**关键点/坑**：
- 坑1：栅瓣 = "另一个方向的信号冒充主瓣目标"——阶段4 干扰能在栅瓣进来。
- 坑2：宽角扫描（θ 大）时 sinθ 变化快，栅瓣风险更大。
**仿真看什么**：AF 随扫描角动画；d=λ/2 vs λ 看栅瓣出现。

## 课3 波束宽度/增益/分辨率互锁
**目标**：理解"口径决定一切"。
**核心原理**：
- 3dB 波束宽 ≈ λ/(N·d) = λ/L（L 口径）。
- 阵列增益 ≈ N（每单元一份功率相干合成）。
- **扫描损失**：指 θ 时波束变宽、增益掉 cosθ；60° 掉 3dB。
**关键点/坑**：
- 坑：两个目标角度差小于波束宽就"看不见"两个——分辨率≈波束宽，除非用超分辨(阶段4课5)。
**仿真看什么**：固定频率改口径长度，波束宽与增益联动曲线。

## 课4 平面阵与构型
**目标**：理解阵面构型=口径/成本/扫描的三角博弈。
**核心原理**：
- 矩形阵（x/y 二维可分离）、圆形阵（对称扫描）、共形阵（贴曲面，波束控制复杂）。
- **子阵**：大阵拆子阵减少后端通道数（每子阵一根射频链）——降成本的核心手段。
- 稀疏阵：随机稀疏可压栅瓣（周期性稀疏出栅瓣，随机化打散）。
**关键点/坑**：
- 坑：子阵化省了硬件但引入"量化栅瓣/子阵栅瓣"，要综合设计。
**仿真看什么**：矩形阵 2D 方向图；子阵划分后方向图变化。

## 课5 锥削与方向图综合
**目标**：懂"低旁瓣的代价"。
**核心原理**：
- 均匀激励副瓣 −13dB——太高，旁瓣会放干扰/杂波进主瓣。
- 锥削：边缘少激励。Chebyshev（等副瓣，给定电平下主瓣最窄）/ Taylor（切趾改良）。
- 代价：主瓣变宽 + 增益降——**副瓣 vs 分辨率/增益折中**。
**关键点/坑**：
- 坑：锥削救不了"栅瓣"，也救不了"单元失效造成的随机旁瓣"——误差预算另算(阶段6课4)。
**仿真看什么**：均匀 vs Taylor vs Chebyshev 方向图叠对比。

## 课6 PESA → AESA → DBF 三代演进
**目标**：从架构史看懂"控制点移进数字世界"。
**核心原理**：
- PESA 无源：集中发射机+阵面只有移相器。便宜、但功率损耗在移相器、单点故障。
- AESA 有源：每单元一个 TR 组件（移相+功放+LNA）。分布式合成、容错、可同时多波束（分区频率）。**现代主力**。
- DBF 数字阵列：每单元 ADC 采下，波束成形在数字域。任意多波束、自适应、单元独立波形（MIMO 入口）。
**关键点/坑**：
- 坑：AESA 的"多波束"是频率分区的近似；DBF 才是真正任意同时多波束——别混淆。
**仿真看什么**：无；做一张三代对比表（控制点/成本/容错/多波束/波形灵活性）。

**阶段3验收**：实现线性阵扫描动画；验证栅瓣公式；锥削前后旁瓣对比。

---

# 阶段4 数字波束成形与阵列算法（L4 会算法）

## 课1 常规波束成形
**目标**：把课2的"移相"升级成"复权重加权求和"。
**核心原理**：
- 窄带 DBF：每个阵元乘复权重 w_n（相位=移相、幅度=锥削）后求和。等于延迟求和的窄带近似。
- 扫描 = 按 θ₀ 设计权重；**同时多波束 = 同时用多组权重**（数字域免费，射频域做不到）。
- 方向图 = wᴴ·a(θ)，a(θ) 为导向矢量。
**关键点/坑**：
- 坑：窄带近似成立的条件是带宽×口径 ≪ c（不然要走宽带延迟线）——宽带相控阵的痛。
**仿真看什么**：8 单元阵，三组权重同时形成三个波束。

## 课2 自适应波束成形 MVDR/Capon
**目标**：懂"自动置零"的数学。
**核心原理**：
- 约束：期望方向增益=1；目标：最小化输出功率（压干扰+噪声）。
- 最优权：**w = R⁻¹a(θ₀) / (aᴴ(θ₀)R⁻¹a(θ₀))**。
- 效果：干扰方向自动深零陷（几十 dB）。常规 BF 只能锥削压副瓣，MVDR 直接"无视"干扰。
- 输出 SINR 最优。
**关键点/坑**：
- 坑1：要估 R，独立快拍 ≥ 通道数，样本不够会崩。
- 坑2：**主瓣方向有偏差 → 信号被当干扰置零**（自消）——引出课3 稳健性。
**仿真看什么**：60° 干扰，常规 BF 方向图副瓣 vs MVDR 在 60° 砸零陷。

## 课3 稳健波束成形（落地才会懂）
**目标**：知道理想 MVDR 为什么碎、工程怎么补。
**核心原理**：
- 理想前提（精确导向矢量、精确协方差）现实全不成立。
- **对角加载**：R + εI，稳但损失一点抑制力——工程标配。
- 白噪声增益约束、子空间法、协方差重构。
**关键点/坑**：
- 坑：稳健性和抑制力是零和博弈，参数 ε 要按误差量级配。
**仿真看什么**：加导向矢量误差，理想 MVDR 自消 vs 对角加载存活。

## 课4 方向图综合进阶（置零约束）
**目标**：把"低旁瓣 + 指定置零"一次配齐。
**核心原理**：
- LCMV：多个线性约束（主瓣=1、干扰方向=0）下最小化输出功率。
- 凸优化(scipy)可做带约束综合。
**关键点/坑**：
- 坑：约束越多自由度越少，过度约束方向图会畸变——要约束点数 vs 阵元数权衡。
**仿真看什么**：给定两个干扰方位，综合出"低旁瓣+双零点"方向图。

## 课5 DOA 估计 MUSIC/ESPRIT
**目标**：实现超分辨测角。
**核心原理**：
- 瑞利限：常规波束扫描分辨力≈波束宽。
- **MUSIC**：R 特征分解 → 信号子空间 E_s / 噪声子空间 E_n；导向矢量正交于 E_n。
  - 谱 **P(θ) = 1/(aᴴ(θ)E_nE_nᴴa(θ))**，分母→0 处=来波方向。
  - 超分辨：波束宽内的两个目标可分。
- ESPRIT：两个子阵旋转不变性 → 特征分解直接得角度，免谱搜索。
**关键点/坑**：
- 坑1：MUSIC 对**相干源**（多径）失效，需空间平滑。
- 坑2：信源数要估计（AIC/MDL），数错全错。
**仿真看什么**：两个 5° 内目标，常规 BF 一个峰 vs MUSIC 两峰；相干源时 MUSIC 崩→平滑后恢复。

## 课6 阵列误差与校准
**目标**：知道"没校准的相控阵=没上保险"。
**核心原理**：
- 幅相误差/互耦/阵元失效 → 方向图畸变、DOA 偏差、栅瓣污染。
- 校准：测已知信号反推幅相误差补偿（有源校准、内置校准网络）。
**关键点/坑**：
- 坑：校准时序和阵面工作要协调（在 AESA 里是系统级设计，不是后处理）。
**仿真看什么**：随机幅相误差注入 → 方向图劣化；补偿后恢复。

**阶段4验收**：MVDR 对拍 vs 常规 BF；MUSIC 分辨两目标；讲清 MVDR 强在哪贵在哪。

---

# 阶段5 检测、跟踪与全链路（L5 会系统）

## 课1 检测理论（奈曼-皮尔逊）
**目标**：懂"检测=有噪下的二选一"。
**核心原理**：
- 两类错误：漏检（降 P_d）、虚警（升 P_fa）。
- N-P 准则：**固定 P_fa，最大化 P_d**。门限由噪声分布定。
- ROC 曲线：P_d vs P_fa 随 SNR 变——检测器性能画像。
**关键点/坑**：
- 坑：虚警率不是可以随便设小的——设太低会漏目标，阈值选择是工程权衡。
**仿真看什么**：给定 SNR，扫门限画 ROC；不同 SNR 的 ROC 族。

## 课2 恒虚警检测 CFAR
**目标**：实现"环境自适应门限"。
**核心原理**：
- 环境杂波变化 → 固定门限失效。CFAR 用目标周围**参考单元**估本地功率设门限，保虚警率恒定。
- CA-CFAR（单元平均）：简单，但边缘/多目标污染参考单元会失效。
- GO（取大）抗边缘、SO（取小）抗密集目标；**OS-CFAR（排序）抗多目标**。
**关键点/坑**：
- 坑1：目标靠太近互相"吃掉"（遮挡）——参考窗和保护单元要配。
- 坑2：均匀噪声下 CA 最优，非均匀下 OS 更稳——没有免费午餐。
**仿真看什么**：两目标相邻，CA 漏检 vs OS 不漏。

## 课3 完整接收链路
**目标**：把阶段1-2 串成一条标准流水线。
**核心原理**：发射波形 → 回波+杂波+噪声 → 脉压 → 多普勒 FFT → CFAR → 点迹。每个环节是前面某课的成品，组合起来就是一部雷达的信号处理"主干道"。
**关键点/坑**：
- 坑：链路里每个模块有参数（脉宽/带宽/PRF/参考窗），改一个牵连全局——这就是阶段6 系统设计的对象。
**仿真看什么**：造一个含杂波+噪声的场景，全链路跑一遍，输出检测点迹。

## 课4 点迹提取与数据关联
**目标**：解决"多点迹→哪个目标"。
**核心原理**：
- 检测输出是距离-速度-角度点迹；关联 = 把点迹配对成目标。
- 最近邻(NN)：就近配，简单但错配多；**JPDA**：联合概率分配，多目标歧义的概率建模。
**关键点/坑**：
- 坑：杂波虚警点、目标分裂、机动转弯——关联是雷达数据处理里最脏最难的活。
**仿真看什么**：多目标+杂波点，NN vs JPDA 航迹质量对比。

## 课5 目标跟踪（卡尔曼）
**目标**：实现预测-量测-更新闭环。
**核心原理**：
- 状态（位置,速度）→ 预测 → 量测 → 更新。**卡尔曼 = 高斯假设下的最优递归估计**。
- α-β 是恒增益特例；EKF/UKF 处理非线性（极坐标量测）。
- 多目标：起批/维持/撤销 + 关联（课4）→ 航迹管理。
**关键点/坑**：
- 坑：模型不匹配（目标机动）就发散——机动检测/变增益是工程关键。
**仿真看什么**：加噪量测 → 卡尔曼滤波比测量平滑、误差收敛；机动目标看发散。

## 课6 相控阵资源管理
**目标**：理解"天线不动，时间是稀缺资源"。
**核心原理**：
- 一个波束同一时刻只指一个方向（DBF 除外）→ 时间要排：搜索空域、跟踪目标、波形确认、校准。
- TWS 边搜边跟；自适应调度按目标优先级/紧迫度分时间。
- **调度算法决定一个阵面能同时应付多少目标**——相控阵的"软实力"。
**关键点/坑**：
- 坑：过度优化调度（把时间全给高价值目标）会让搜索空域漏目标——搜索-跟踪是零和。
**仿真看什么**：模拟调度器时间轴，多目标优先队列下波束分配动画。

**阶段5验收**：完整闭环（发射→检测→跟踪）仿真跑通并画航迹。

---

# 阶段6 硬件与系统设计深潜（L6 会硬件）

## 课1 发射链路
**目标**：懂"功率与热是发射机的天花板"。
**核心原理**：
- 固态功放(SSPA)替代行波管：可靠、**相参发射**（相干处理前提）。
- 峰值 vs 平均功率：平均=峰值×占空比；脉压用长脉宽提平均功率，但散热限占空比。
**关键点/坑**：
- 坑：占空比不是随便提的——功放热损坏是实战事故主因之一。
**仿真看什么**：占空比/脉宽/PRF 与平均功率的关系曲线。

## 课2 接收链路与噪声系数级联
**目标**：背下并会用级联公式。
**核心原理**：

$$F_{总} = F_1 + \frac{F_2-1}{G_1} + \frac{F_3-1}{G_1 G_2} + \cdots$$

- **第一级 LNA 定生死**：F1 小、G1 大 → 后级噪声被摊薄。LNA 要贴天线、馈线损耗要小。
- 增益分配要算每级动态范围/1dB 压缩点。
**关键点/坑**：
- 坑：天线到 LNA 的馈线损耗直接加进 F1——"天线后面第一样东西必须是低噪放"。
**仿真看什么**：级联噪声系数 vs 第一级增益扫描，看摊薄效果。

## 课3 采样架构与 ADC
**目标**：懂 ADC 指标怎么砸系统。
**核心原理**：
- 中频采样 / 直接射频采样（带通采样）；DDC 数字下变频降数据率。
- **ENOB**：SNR = 6.02·ENOB + 1.76 dB，多 1 位 +6dB。
- **SFDR**：杂散抑制——宽带相控阵的梦魇（带内杂散冒充目标）。
**关键点/坑**：
- 坑：采样率提上去数据率爆炸 → ADC 后面 FPGA 资源是瓶颈，"高采样"不是免费午餐。
**仿真看什么**：量化噪声 vs 位数；带通采样选 fs 避免混叠。

## 课4 TR 组件与阵面硬件
**目标**：看懂 AESA 的心脏和它的误差预算。
**核心原理**：
- TR = 移相器+功放+LNA+收发开关/环形器+幅相控制。
- 成千上万个组件的**一致性/温度漂移 → 幅相误差预算**，超了方向图/DOA 全崩（阶段4课6 校准为它服务）。
- 收发隔离、热管理（高功率密度+极难散热）。
**关键点/坑**：
- 坑：TR 误差是"统计的"——随机误差抬底噪、系统误差偏方向，两类分开管。
**仿真看什么**：TR 幅相误差注入 → 方向图抬副瓣/主瓣偏，蒙特卡洛看统计。

## 课5 FPGA/DSP 实时处理架构
**目标**：懂"算法要落进硬件约束"。
**核心原理**：
- 脉压(FFT)→多普勒 FFT→CFAR 流水线；**吞吐 = 采样率×通道数**。
- 定点/并行/时序：算法要在 FPGA 里分块、定点化、按时序排。
- 实时性 vs 延迟预算：资源调度(阶段5课6)要毫秒级响应。
**关键点/坑**：
- 坑：定点化引入量化损失——"和仿真差 2dB"往往就是定点 vs 浮点。
**仿真看什么**：把浮点脉压链转定点，对比输出损失（可先用 numpy 的 float32 模拟）。

## 课6 系统设计实战（指标反推）
**目标**：从需求倒推整机，输出链路预算。
**核心原理**：需求(R_max, σ, 更新率) → 雷达方程定 P_t×G 组合 → 定频段/阵面/TR 数 → 定 PRF/脉宽/带宽（模糊性权衡）→ 定 ADC/处理资源 → **链路预算表**（每个损耗/余量摊开）。
**关键点/坑**：
- 坑：预算表里"余量"留多少是工程艺术——留少了恶劣天气掉链子，留多了浪费。
- ⚠️ 边界：功放非线性、噪声系数实测、实物方向图必须真硬件；链路预算/性能预估/信号流仿真可信。
**仿真看什么**：造一张可调参数的链路预算表，改一栏看探测距离联动。

**阶段6验收**：完成一份 8 通道 AESA 系统设计文档（链路预算+选型理由+性能预估）。

---

# 阶段7 前沿与综合（L7 会前沿）

## 课1 MIMO 雷达
**目标**：分清相控阵 vs MIMO 的本质。
**核心原理**：
- 相控阵：N 阵元同一波形相干合成，增益 N，一个波束。
- MIMO：N_t 个正交波形可分离 → **虚拟阵列 N_t×N_r** → 自由度按乘积涨、分辨率更高。
- 代价：损失部分相干增益。
**关键点/坑**：
- 坑：正交波形在工程上难做完美（互相关残留），接收分离不干净会"串扰"。
**仿真看什么**：单发多收 vs 多发多收的虚拟孔径 → 波束窄度对比。

## 课2 STAP 空时自适应
**目标**：理解"空间+时间联合自适应"。
**核心原理**：
- 机载下视：地杂波随平台速度展成**杂波脊**（各角度地物不同多普勒），固定 MTI 滤不掉。
- STAP：多个脉冲(时间)×多个阵元(空间)联合自适应，沿杂波脊置零，只留动目标。
- 计算量大 → 降维/约化（部分自适应）。
**关键点/坑**：
- 坑：STAP 训练数据要含杂波统计且目标不在训练样本里——"污染"问题。
**仿真看什么**：小规模（4 阵元×8 脉冲）STAP，杂波脊置零前后 RD 图对比。

## 课3 认知雷达
**目标**：懂"发射也在适应"。
**核心原理**：
- 传统：固定波形固定算法。认知：**感知环境→自适应选波形→再感知**闭环。
- 模糊函数(阶段2课5)在此兑现：不同环境选不同波形形状。
**关键点/坑**：
- 坑：闭环要快（波形决策延迟），否则环境已变，越"聪明"越乱。
**仿真看什么**：模拟干扰方向变化，认知雷达换波形/换置零方向 vs 固定雷达性能对比。

## 课4 电子对抗视角
**目标**：从敌人视角看自己的雷达。
**核心原理**：
- 干扰：压制式(噪声)、欺骗式(假目标/距离拖引)、DRFM 转发。
- 对抗：频率捷变、旁瓣对消/置零(阶段4)、LPI(低截获——波形扩散)。
**关键点/坑**：
- 坑：DRFM 能复制你波形后"回放"，匹配滤波对复制干扰无效——要靠波形随机化/捷变。
**仿真看什么**：注入欺骗干扰，对比常规检测 vs 频率捷变/旁瓣对消后的检测。

## 课5 雷达组网与软件化
**目标**：看雷达从"一台机器"到"一个平台"。
**核心原理**：
- 组网/多基地：一发多收，抗干扰抗隐身（双站视角 σ 变化）。
- 软件化/开放架构：通用硬件+软件定义功能。
**关键点/坑**：
- 坑：组网的时间/相位同步是难题——同步不好，多站数据融合反而变差。
**仿真看什么**：双基地 σ 对比单基地（球 vs 平面波散射的差异）。

## 课6 论文研读与复现
**目标**：检验"专家级"——能读+能复现。
**核心原理**：读 IEEE TAES/TSP 的路子：摘要→问题→方法→仿真→局限；复现核心仿真是对理解的终考。
**关键点/坑**：
- 坑：论文复现失败 90% 是参数没读全（场景设定）而非算法错——先还原"场景表格"。
**仿真看什么**：选一篇开源的雷达论文（如某种波形/检测方法），完整复现其仿真图。

## 综合项目：多功能相控阵雷达
**目标**：七阶段所有线收拢成一份作品。
**要求**：需求→频段/阵面/波形→发射/接收→波束成形→检测→跟踪→资源调度→性能评估→局限，输出完整技术报告（含仿真图和决策理由）。
**验收**：报告答辩——每层设计能讲清"为什么这么选、代价是什么、换一个会怎样"。

---

# 贯穿三条主线（收束）

1. **能量线**：雷达方程(阶段1课2)→噪声系数级联(阶段6课2)——信号一路衰减，每级在打能量仗。
2. **自由度线**：相位差控波束(阶段3)→自适应置零/DOA(阶段4)→空时联合/虚拟阵列(阶段7)——自由度越多看得越多。
3. **模糊性线**：采样混叠(阶段2课1)→距离-速度模糊(阶段2课6)→栅瓣(阶段3课2)→旁瓣(阶段3课5)——**所有坑是同一种：别处来的信号冒充目标**。雷达工程师的一生是与"假冒"斗争。

---

## 学习节奏建议（每周10h+）

- 阶段1: 3周 · 阶段2: 4周 · 阶段3: 3-4周 · 阶段4: 4-5周 · 阶段5: 4-5周 · 阶段6: 4-5周 · 阶段7: 5-6周（含综合项目）
- 每课 = 读讲义 + 跑"仿真看什么" + 做练习 + 过验收，再进下一课。
- 阶段末综合实战 + 复盘（问题→原因→解决）。
